# AgriConnect Frontend Environment Variables
# Copy this file to .env and fill in your values
#
# NOTE: Create React App requires environment variables to be prefixed with REACT_APP_
# Variables without this prefix will not be available in the browser.

# ===========================================
# API CONFIGURATION
# ===========================================

# Backend API URL
# - For local development: http://localhost:5000/api
# - For production (Render): https://agriconnect-web-mvp.onrender.com/api
#
# IMPORTANT: Include the full URL with /api suffix
REACT_APP_API_URL=http://localhost:5000/api

# File Upload URL (for serving uploaded images)
# - For local development: http://localhost:5000/uploads
# - For production (Render): https://agriconnect-web-mvp.onrender.com/uploads
REACT_APP_UPLOAD_URL=http://localhost:5000/uploads

# ===========================================
# DEPLOYMENT CHECKLIST
# ===========================================

# For Vercel deployment:
# 1. Go to Project Settings > Environment Variables
# 2. Add REACT_APP_API_URL = https://agriconnect-web-mvp.onrender.com/api
# 3. Add REACT_APP_UPLOAD_URL = https://agriconnect-web-mvp.onrender.com/uploads
# 4. Redeploy for changes to take effect

# Troubleshooting:
# - If API calls fail on mobile but work on desktop, check:
#   1. Backend CORS is configured with your Vercel URL
#   2. REACT_APP_API_URL uses HTTPS (not HTTP) in production
#   3. No trailing slashes in URLs
# - Check browser console for CORS errors
# - Verify the API is accessible: curl https://your-backend.onrender.com/api/health
